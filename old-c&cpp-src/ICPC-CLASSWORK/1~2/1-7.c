#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    double length = 6; double width = 9;
    printf("%.4f \n", sqrt(length * length + width * width ));

    system("pause");
    return 0;
}