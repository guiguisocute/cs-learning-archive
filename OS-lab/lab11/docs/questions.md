# 思考题

1. 请求分页系统中，操作系统必须额外维护哪些信息，才能在缺页时做出置换决策？
2. FIFO 为什么可能出现性能不稳定的问题？它忽略了什么页面行为信息？
3. 题面把第二个算法写成 `LRU(LFU)`，但文字又说“最长时间没有被引用”。你能解释这更接近哪一种算法吗？
4. 如果页面访问串具有明显局部性，LRU 为什么通常会优于 FIFO？
5. 在你的实现中，哪一个数组最能体现“每一步过程都要被记录”这一实验要求？
6. 如果以后继续扩展 `OPT` 或真正的 `LFU`，你认为当前工程结构还需要新增哪些字段或函数？
