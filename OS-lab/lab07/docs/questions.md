# 实验思考题

1. 固定分区分配想解决的核心问题是什么？
2. 系统必须记录哪些信息，才能决定一个作业是否能够装入某个分区？
3. 为什么 30KB 的作业不能装入 12KB 的分区，却能装入 32KB 的分区？
4. 这个实验里最直观的空间浪费体现在哪里？
5. 如果多个分区都能装下同一个作业，为什么当前实现会优先选择分区表中更靠前的分区？
6. 若某个作业输入为 `2`，程序为什么仍然可以定位到 `JOB2`？
7. 如果把固定分区改成动态分区，你认为“状态”字段是否还够用？
