#include<stdio.h>
#include<stdlib.h>

int main(){
    int m,n,temp,r;
    scanf("%d %d",&m,&n);

    if(m < n);{
        temp = m;
        m = n;
        n = temp;
    }
    while(m % n != 0){
        m %
    }

}