#include<stdio.h>
#include<stdlib.h>

int main(){
    int a;
    scanf("%d",&a);
    if(a <= 1 ){
    printf("Today, I ate %d apple.",a);
    
    system("pause");
    return 0;
    }
    else{
        printf("Today, I ate %d apples.",a);

         system("pause");
         return 0;
    }
}